---
name: Lint Build Publish

on: # yamllint disable-line rule:truthy
  push:
  pull_request:

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Super-linter
        uses: super-linter/super-linter@4758be622215d0954c8353ee4877ffd60111cf8e # v6.4.1
        env:
          VALIDATE_JSCPD: false
          FILTER_REGEX_EXCLUDE: >-
            \.sample-secret\.md|\/config\/(shell_gpt\/roles\/|weechat\/.*(pl|py|lua)|awesome\/.*lua)|\/scripts\/supermicro_ipmi\.sh|notes\/.*\.md
      - name: Check .zshrc source idempotency
        run: ./test/source_idempotency.zsh

  validate-images:
    name: Validate ${{ matrix.distro }} image
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        distro: [archlinux, fedora, gentoo, debian, brew]
    steps:
      - name: Build exact triggering revision
        uses: docker/build-push-action@v7
        with:
          context: ${{ github.server_url }}/${{ github.repository }}.git#${{ github.sha }}
          file: Dockerfile.${{ matrix.distro }}
          build-args: |
            REPO_URL=${{ github.server_url }}/${{ github.repository }}.git
            COMMIT_SHA=${{ github.sha }}
          push: false

  publish-images:
    name: Publish ${{ matrix.distro }} image
    if: github.event_name == 'push' && github.repository_owner == 'maxattax97'
    needs: [lint, validate-images]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - distro: archlinux
            tags: latest,archlinux
          - distro: fedora
            tags: fedora
          - distro: gentoo
            tags: gentoo
          - distro: debian
            tags: debian
          - distro: brew
            tags: brew
    steps:
      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: dotfiles
          tags: ${{ matrix.tags }}
      - name: Build and push exact validated revision
        uses: docker/build-push-action@v7
        with:
          context: ${{ github.server_url }}/${{ github.repository }}.git#${{ github.sha }}
          file: Dockerfile.${{ matrix.distro }}
          build-args: |
            REPO_URL=${{ github.server_url }}/${{ github.repository }}.git
            COMMIT_SHA=${{ github.sha }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
