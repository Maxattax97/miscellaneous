#!/bin/sh
# Calm down, this is not some kind of virus, it's just a script which copies my
# personal config's over to remote machines.

#curl https://raw.githubusercontent.com/Shougo/dein.vim/master/bin/installer.sh | sh -s ~/.cache/dein

tar xcvf ./.infect.tar.gz -C ~/
